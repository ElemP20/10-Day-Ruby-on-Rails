<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Airline Customer Class Management" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  
  <body class="min-h-screen bg-gray-100">
    <nav class="bg-white shadow-md">
      <div class="max-w-4xl mx-auto px-4 py-3 flex justify-between items-center">
        <div>
          <%= link_to "Home", root_path, class: "text-lg font-semibold" %>
        </div>
        <div class="space-x-4">
          <% if logged_in? %>
            <%# Only show the class page that matches the current user role %>
            <% case current_user.role.to_s %>
              <% when 'economy' %>
                <%= link_to "Economy Lounge", economy_path, class: "hover:text-blue-600" %>
              <% when 'business' %>
                <%= link_to "Business Lounge", business_path, class: "hover:text-blue-600" %>
              <% when 'first_class' %>
                <%= link_to "First Class Lounge", first_class_path, class: "hover:text-blue-600" %>
            <% end %>
            <span class="text-gray-700">Logged in as <%= current_user.email %> (<%= current_user.role %>)</span>
            
            <%= link_to "Logout", logout_path, data: { turbo_method: :delete }, class: "ml-4 text-red-500" %>
          <% else %>
            <%= link_to "Login", login_path, class: "hover:text-blue-600" %>
            <%= link_to "Signup", signup_path, class: "hover:text-blue-600 ml-2" %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="max-w-4xl mx-auto px-4 mt-6">
      <% flash.each do |type, message| %>
        <div class="p-3 mb-4 rounded 
            <%= type == 'notice' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
